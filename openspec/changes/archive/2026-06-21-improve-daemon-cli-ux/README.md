# improve-daemon-cli-ux

Improve chorus daemon CLI: interactive credential completion, default YOLO with confirm/ack, richer startup banner + per-wake logs, --agent reservation, and -d background lifecycle
