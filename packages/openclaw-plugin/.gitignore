# Build output — regenerated by `npm run build` / `prepublishOnly`, shipped to
# npm via the package.json `files` allowlist, never committed.
dist/

# Dependencies (this package is outside the root pnpm workspace, so it has its
# own node_modules).
node_modules/

# TypeScript incremental build info
*.tsbuildinfo

# npm pack output
*.tgz
